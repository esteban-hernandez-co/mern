const HOME = "/";

const AUTH_ROOT = "/auth";
const AUTH_LOGIN = AUTH_ROOT + "/login";
const AUTH_FORGOT = AUTH_ROOT + "/forgot";
const AUTH_RESET = AUTH_ROOT + "/reset";
const AUTH_SIGNUP = AUTH_ROOT + "/signup";

const USERS_ROOT = "/users";

const ARTIST_ROOT = "/artist";
const ARTIST_MANAGEMENT = ARTIST_ROOT + "/management";

const LABEL_ROOT = "/artist";
const LABEL_MANAGEMENT = LABEL_ROOT + "/management";

export const PATHS = {
  HOME,
  AUTH_LOGIN,
  AUTH_FORGOT,
  AUTH_RESET,
  AUTH_SIGNUP,
  USERS_ROOT,

  ARTIST_ROOT,
  ARTIST_MANAGEMENT,
  LABEL_ROOT,
  LABEL_MANAGEMENT,
};
